<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- ID -->
        <TextView
            android:id="@+id/textEntityId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="(ID)"
            android:textStyle="bold" />

        <!-- Title -->
        <TextView
            android:id="@+id/textEntityTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/textEntityId"
            android:layout_marginStart="8dp"
            android:text="Название"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Additional info -->
        <TextView
            android:id="@+id/textEntityInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/textEntityTitle"
            android:layout_alignStart="@id/textEntityTitle"
            android:text="Дополнительная информация"
            android:textSize="14sp"
            android:layout_marginTop="4dp" />

        <!-- Edit icon -->
        <ImageView
            android:id="@+id/iconEdit"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/edit_icon"
            android:contentDescription="Edit" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>
